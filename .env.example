# Product World Interface Kit Environment Configuration

# APP_URL: The URL where this applet is hosted.
APP_URL="MY_APP_URL"

# Optional, development-only Tripo API credential. Never expose it through a
# VITE_ variable or commit the real value. The asset candidate CLI reads this
# key only from Node.js: npm run assets:tripo -- --help
TRIPO_API_KEY=""
